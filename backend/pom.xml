<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.0</version>
        <relativePath/>
    </parent>

    <groupId>com.dj.ai</groupId>
    <artifactId>dj-agent-chat-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>dj-agent-chat-parent</name>
    <description>Spring AI + Volcengine Ark chat —— 多模块聚合父 POM</description>

    <modules>
        <!-- 纯 Java 工具扩展点（BUILTIN 插件 SPI，零框架依赖） -->
        <module>agentchat-tools-spi</module>
        <!-- 工具插件聚合：以后每个 Java 工具包在 tools/ 下新增一个子模块 -->
        <module>tools</module>
        <!-- 可启动应用（唯一打 fat jar 的模块） -->
        <module>agentchat-app</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <spring-ai.version>1.0.0-M7</spring-ai.version>
    </properties>

    <!-- M7 仅发布在 milestone 仓库 -->
    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots><enabled>false</enabled></snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <!-- 统一管理全部 spring-ai 工件版本 -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- 本仓工具 SPI：插件模块与 app 统一版本 -->
            <dependency>
                <groupId>com.dj.ai</groupId>
                <artifactId>dj-agent-tools-spi</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
