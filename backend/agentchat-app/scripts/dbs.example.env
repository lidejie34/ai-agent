# QA 只读账号凭据模板。复制到 dev-local/troubleshoot/dbs.env（仓内 gitignore 隔离）后填写真实密码。
# 该文件严禁提交、严禁回显到工具输出（可用 AI_AGENT_CONF_DIR 把整个配置目录指向别处）。
# 密码来源：~/.cursor/mcp.json 中同名 MCP 连接（user-mysql-drp-qa / user-mysql-drp-order-qa）。
TETITCDRP_QA_PWD=
TETDORDER_QA_PWD=
